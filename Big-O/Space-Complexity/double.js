function double(arr) {
  let newArr = []; // 1
  for (let i = 0 /* 1 */; i < array.length; i++) {
    newArr.push(2 * arr[i])    
  }
  return newArr;
} // O(n) the space it takes depend on the array length from  the params